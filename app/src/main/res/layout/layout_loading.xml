<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/group"
    android:background="@color/background_20"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        android:id="@+id/pbCircular"
        android:layout_width="@dimen/loading_height"
        android:layout_height="@dimen/loading_height"
        android:indeterminateOnly="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.40" />

    <TextView
        android:id="@+id/tvLoading"
        style="@style/TextAppearance.Loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_regular"
        app:layout_constraintEnd_toEndOf="@+id/pbCircular"
        app:layout_constraintStart_toStartOf="@+id/pbCircular"
        app:layout_constraintTop_toBottomOf="@+id/pbCircular"
        tools:text="Loading data..." />

</androidx.constraintlayout.widget.ConstraintLayout>